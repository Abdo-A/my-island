import React, { Component } from "react";
import "./Events.css";

export default class Events extends Component {
  state = {
    events: null,
    error: null
  };

  render() {
    return <div>Events</div>;
  }
}
